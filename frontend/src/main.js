import { createApp } from 'vue'
import App from './App.vue'
import './assets/global.css';

//creates the Vue app instance and attaches it to the div with id "app" in index.html
createApp(App).mount('#app')
